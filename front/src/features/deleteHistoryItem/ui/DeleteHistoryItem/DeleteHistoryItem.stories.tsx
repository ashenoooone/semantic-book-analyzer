import { DeleteHistoryItem } from './DeleteHistoryItem';
import type { Meta, StoryObj } from '@storybook/react';

const meta: Meta<typeof DeleteHistoryItem> = {
  title: 'DeleteHistoryItem',
  component: DeleteHistoryItem,
};

export default meta;
type Story = StoryObj<typeof DeleteHistoryItem>;

export const Default: Story = {
};